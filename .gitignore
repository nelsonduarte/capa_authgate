# Vendored dependencies are fetched by `capa install` and pinned by
# capa.lock; they are not committed.
vendor/

# Python bytecode (if anyone runs the transpiled code directly)
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Local probes / scratch
_*.capa
